#!/usr/bin/env bash

. "$( dirname "${BASH_SOURCE[0]}" )/base_call.sh"
. "$( dirname "${BASH_SOURCE[0]}" )/pangenome_data_gen.sh"
. "$( dirname "${BASH_SOURCE[0]}" )/curves_matrix.sh"
. "$( dirname "${BASH_SOURCE[0]}" )/rgp_gen.sh"
. "$( dirname "${BASH_SOURCE[0]}" )/spots_of_insertion.sh"
. "$( dirname "${BASH_SOURCE[0]}" )/rarefaction_curves.sh"
. "$( dirname "${BASH_SOURCE[0]}" )/partitions_projections_families.sh"
. "$( dirname "${BASH_SOURCE[0]}" )/fasta_outputs.sh"
. "$( dirname "${BASH_SOURCE[0]}" )/phylogeny.sh"

